server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://141.164.56.77:3306/project?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: project
    password: projectdb
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    generate-ddl: false
    show-sql: true
    open-in-view: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
      devtools:
    livereload:
      enabled: true
    servlet:
      multipart:
        file-size-threshold: 1MB
        max-file-size: 100MB
        max-request-size: 100MB

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
